# KE_BLOCK user-approved visual baseline — 2026-09-19

The corrected DOCX and PDF have now crossed the human visual gate.

Explicit approval:
> Approve docx and pdf !

The accepted pair is frozen as the visual regression baseline:

- DOCX SHA-256: `e936ffcb89eb7da13b8be8d447c8453b5b4499a4907390816846f5c8ec94a6b9`
- PDF SHA-256: `654e74630861b5b398ab577d55da5f692bf4549b0af480498822428109353b02`
- artifact bundle: `sha256:ce9c806aebcce0cc9f15eaaf728c4e2bb30e4e93cb2db26b80c9e8c3ce8db8f0`

The baseline is visual/render authority for regression only. JSON remains semantic SSOT.

## New first-red

`GOVERNED_STYLE_SYSTEM_CANDIDATE`

The next candidate must move beyond the standard-blue look while keeping style choices granular and tokenized:

- body / heading / mono font families;
- body, heading, caption and metadata sizes;
- semantic palette roles rather than hard-coded colors;
- special heading/section numbers;
- requirement IDs and metadata labels;
- captions and cross-reference number styling;
- tables, callouts and spacing;
- render-diff against the approved baseline.

No style candidate can replace the accepted baseline without a new explicit user approval.
