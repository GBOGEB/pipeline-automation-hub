# HM-01 R3 / W275 — MissionControl Lossless Federation Handover

This is an orchestration/navigation receipt. It does not replace QPS engineering
authority, CODEX publication authority, ABACUS runtime evidence authority, or
GEMINI producer evidence.

## Current recursive chain

MissionControl #199
→ QPS #1451
→ QPS #1452
→ GEMINI #17
→ QPS #1453
→ QPS #1460
→ ABACUS #1263
→ this MissionControl federation receipt
→ next physical dependency
→ QPS 3PC Prove
→ QPS acceptance
→ R4 only after PASS.

## Burned progress

The exact-environment half of R3 physical ingress is now real, not merely
planned. GEMINI run `35338333168` / job `105578279671` passed, including a
relocated self-test under Python 3.12.14 with the six exact governed dependency
versions. Artifact `10543767484` is bound by digest
`sha256:9a961d69becd02bac3c2c16146f29ced1a9185204cce2e1f381eddfb6465aaa5`.

QPS #1453 already contains the consumer path that verifies the capsule, creates
the exact detached successor worktree, regenerates the five products, runs
structural/parity/render checks and stops for hash-bound visual inspection
before the successor acceptance validator.

QPS #1460 publishes the recursive history/dependency/restart boundary and
merged at `34d21db38a0cfc2a07c4d59039e002bdb89578fa`.

CODEX #756 repaired QPS-v6 Pages authority but does not claim publication PASS.

ABACUS current-pin support evidence is real on exact main SHA
`f55876e4d7985dd672304ff6e3945f24de9b9194`, run `35272680285`.
Five QPS-facing jobs passed and pytest reported 402 passed / 0 failed from 415
collected. Artifact `10518602763` is bound by SHA-256
`2904d3cc2ef3f359b98db0b75bd2613436b94395b975ccf807c08a8c1d793b7f`.
Legacy Deploy Artifacts failed separately and remains ABACUS governance drift.
PR #1263 binds that distinction.

## First red

Only one R3 physical prerequisite remains:
a genuine Git-object-bearing handoff containing exact active production
successor `1248290ca0a9d55ec83d0efa0235ed1a45a88eeb`.

The old `70964e5f...` handoff target is historical predecessor provenance and
must not be relabelled as current proof.

## Execution after return

Verify the real Git object and pair it with the already-proven exact-environment
capsule. Run the existing QPS portable-capsule consumer unchanged. Bind the
exact input hashes, exact worktree SHA, five product hashes, structural QA,
projection-aware parity, render evidence, explicit hash-bound human visual
receipt and successor acceptance result.

Only an actual `PASS_R3_RELEASE_PRODUCTION_DOV` permits R4.

## Immutable guards

#923 remains RED_OWNER_ACTION. W267 / GT_BDQ_0 remains
RED_BLOCKED_ON_923_INFRA_PREEXECUTION. R3 re-proves release capability only and
does not inherit or burn canonical GT_BDQ_4–7. Runtime GOLD is withheld.
Engineering, negotiation and formal credit deltas remain zero.
Authority transfer remains false.
