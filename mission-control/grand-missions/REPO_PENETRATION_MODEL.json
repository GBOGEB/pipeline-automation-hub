{
  "schema": "missioncontrol.repo_penetration_model.v1",
  "purpose": "separate fleet breadth from repository penetration depth and runtime proof",
  "axes": {
    "breadth": {
      "B0": "unknown/unseen repository",
      "B1": "identity known",
      "B2": "included in MissionControl estate census",
      "B3": "classified into mission/reserve/tail disposition",
      "B4": "has named Scout/Reader return path",
      "B5": "has accepted cross-feed consumer or explicit NO_CROSS_FEED_JUSTIFIED",
      "B6": "under recurring CONTROL/recensus"
    },
    "penetration": {
      "P0": "repo metadata only",
      "P1": "root README/default branch/exact head",
      "P2": "root files + hidden control dirs such as .github/.devcontainer/.vscode + primary manifests",
      "P3": "first-level modules/packages/apps/tests/docs/examples/scripts and workflow surfaces mapped",
      "P4": "recursive tree census including nested hidden dirs, generated/config surfaces, nested manifests and candidate subprojects",
      "P5": "subrepo/submodule/workspace/monorepo boundaries resolved; nested packages or embedded repos classified individually",
      "P6": "execution path traced from entrypoint through dependencies/workflows to outputs/receipts",
      "P7": "multiple meaningful execution paths + failure/edge paths exercised; cross-module behavior observed",
      "P8": "CONTROL depth: repeated exact-SHA runtime plus drift/recensus and accepted federation returns"
    },
    "runtime": {
      "R0": "no runtime evidence",
      "R1": "runtime surface identified only",
      "R2": "static validation/lint/config parse or cheap non-domain probe",
      "R3": "one bounded >0-step execution at exact source identity",
      "R4": "deterministic repeat or same-path repeat at distinct evidence point",
      "R5": "integration execution across module/subrepo boundary",
      "R6": "failure/negative/edge path observed and attributed",
      "R7": "recurring CONTROL runtime with telemetry and drift detection"
    }
  },
  "nested_surface_rules": [
    "dot-prefixed directories are first-class surfaces, not ignored noise",
    "detect .gitmodules, nested package manifests, workspace files, Dockerfiles, devcontainers, workflows, scripts, tests, docs, examples and generated folders",
    "a nested package or embedded project receives its own P/R state when it has an independent manifest, workflow, build, runtime or authority boundary",
    "large upstream repositories may be breadth-covered while intentionally shallow-penetrated until value justifies deeper traversal",
    "P4+ requires recursive structure evidence rather than README inference",
    "P6+ requires traced execution evidence; source reading alone cannot satisfy runtime depth"
  ],
  "reporting_contract": {
    "per_repo": ["breadth_level", "penetration_level", "runtime_level", "max_tree_depth_observed", "hidden_dirs_seen", "nested_project_boundaries", "submodule_state", "workflow_count_or_surface", "runtime_paths_proven", "coverage_gaps", "next_depth_action"],
    "fleet": ["repo_breadth_fraction", "P0_to_P8_histogram", "R0_to_R7_histogram", "weighted_depth_score", "hidden_surface_coverage", "nested_boundary_coverage", "runtime_coverage", "control_coverage"]
  },
  "anti_overclaim": "BREADTH_COVERAGE_NEVER_IMPLIES_PENETRATION_OR_RUNTIME_COVERAGE"
}
